<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<elastic-credentials>
  <% @hosts.each do |url, host| -%>
  <credentials url="<%= url %>" username="<%= host['username']%>" password="<%= host['password'] %>"/>
  <% end -%>
</elastic-credentials>
