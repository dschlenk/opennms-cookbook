version: 2.1

jobs:
  test:
    machine:
      image: ubuntu-2404:current
    steps:
      - checkout
      - run:
          name: install cinc workstation
          command: curl -L https://omnitruck.cinc.sh/install.sh | sudo bash -s -- -P cinc-workstation -v 24
      - run:
          name: cookstyle
          command: cookstyle
      - run:
          name: kitchen
          command: KITCHEN_LOCAL_YAML=kitchen.dokken.yml kitchen test --destroy always

workflows:
  version: 2
  untagged-build:
    jobs:
      - test
