package com.example.rules

import com.example.model.Profile

rule "Create If Missing Rule - Missing Profile"
    when
        $profile : Profile(missing == true)
    then
        System.out.println("Creating missing profile for: " + $profile.getUserId());
end
