package com.example.rules

import com.example.model.User

rule "Override Rule - Admin Override"
    when
        $user : User(role == "admin" && override == true)
    then
        System.out.println("Admin override triggered by: " + $user.getUsername());
end
